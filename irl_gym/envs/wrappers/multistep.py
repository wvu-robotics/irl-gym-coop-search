#pass in env params, and will wrap and perform multiple timesteps.

#init env.
# then in step function perform multiple timesteps. 
    #idea of doing it this way is that we can still leverage truncation in gym.